#ifndef ERF_HPP 
#define ERF_HPP 


#include <cmath>
#include <limits>
#include <numbers>

#include "approximation/approx.hpp"
#include "types.hpp"


[[nodiscard]]
#if defined(__GNUC__) || defined(__clang__)
__attribute__((hot, always_inline))
#endif
inline f64 calerf(const f64 u) noexcept {

    /* 
          Mathematics of Computation (W. J. Cody, 1969)
              VOL. 23, NO. 107.
     
          ALGO. 715, TOMS (W. J. Cody, 1993) ~SPECFUN
              VOL. 19, NO. 1.
     */
}
#endif
